// Stub: upstream loads pako for deflate/gzip; our decompress helper
// override bypasses it entirely, so this is a no-op placeholder.
globalThis.pako = globalThis.pako || {
  inflate: () => { throw new Error("pako not available; use DecompressionStream"); },
  inflateRaw: () => { throw new Error("pako not available; use DecompressionStream"); },
};
