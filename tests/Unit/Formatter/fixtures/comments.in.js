// file header comment

// leading comment on statement
const a = 1; // trailing same-line

/* block before */ const b = 2;

/**
 * JSDoc style block
 * with lines
 */
function documented() {
  // inside function

  return 1; // trailing in block
}

const obj = {
  // comment above property
  key: 'value',
};
