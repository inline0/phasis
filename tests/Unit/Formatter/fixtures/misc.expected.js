const chained = value?.deep?.prop ?? fallback;
const opt = api?.fetch?.(url);
delete target.prop;
void promise;
typeof thing === 'string' && thing.trim();
label: {
	break label;
}
new Date();
new (factory())();
const seq = (a++, b--, a + b);
throw new Error(`fail: ${code}`);
try {
	risky();
} catch {
	recover();
} finally {
	cleanup();
}
async function waits() {
	await Promise.all([one(), two()]);
}
function* gen() {
	const got = yield produce();
}
