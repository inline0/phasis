for (let i = 0; i < items.length; i++) { total += items[i]; }
for (const [key, value] of Object.entries(map)) { console.log(key, value); }
for (const key in obj) { if (Object.hasOwn(obj, key)) count++; }
do { attempts++; } while (attempts < 3 && !done);
while (queue.length) queue.pop();
outer: for (;;) { break outer; }
