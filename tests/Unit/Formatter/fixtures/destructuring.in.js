const { alpha, beta = 2, gamma: renamed, ...rest } = source;
const [first, , third = 'x', ...tail] = list;
function draw({ size = 'big', coords: { x = 0, y = 0 } = {}, radius = 25 } = {}) { return [x, y]; }
const fn = ({ a, b }, [c, d], ...others) => a + b + c + d + others.length;
