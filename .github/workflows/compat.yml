name: Compatibility Report

on:
  workflow_dispatch:

concurrency:
  group: compat
  cancel-in-progress: false

jobs:
  compat:
    runs-on: ubuntu-latest
    timeout-minutes: 300

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, bcmath, intl
          coverage: none
          ini-values: memory_limit=2G

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Run unit tests
        run: vendor/bin/phpunit --testsuite=unit

      - name: Run full compat report
        run: ./bin/compat-report --jobs 4 --timeout 30 --chunk-size 100
        timeout-minutes: 240

      - name: Show summary
        if: always()
        run: |
          if [ -f COMPAT.md ]; then
            echo "## Compatibility Summary" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            head -30 COMPAT.md >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compat-report
          path: |
            COMPAT.md
            compat.json

      - name: Commit COMPAT.md
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add COMPAT.md compat.json
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Update COMPAT.md from CI [skip ci]"
            git push
          fi
