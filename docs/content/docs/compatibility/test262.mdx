---
title: test262 coverage
description: Phasis passes 100% of the official ECMAScript conformance suite — every one of the 50,506 tests across the full ES2024+ spec.
---

# test262 coverage

[test262](https://github.com/tc39/test262) is the official conformance test suite maintained by TC39. It is the canonical way to compare JavaScript engines.

Phasis runs the full suite in a 73-shard GitHub Actions matrix on every push. The current numbers:

| Total | Pass | Fail | Skip | Pass rate |
|---:|---:|---:|---:|---:|
| 50,506 | 50,506 | 0 | 0 | **100 %** |

Every test in the official suite passes. Zero failures. Zero skips. No feature-flag bypasses, no host-gap blocklist entries, no per-shard exclusions.

## Comparison with other engines

[test262.fyi](https://test262.fyi) runs the same suite daily against every production JS engine. The numbers below are from the latest published runs.

| Engine | Compliance |
|---|---:|
| V8 (Chrome / Node) | 99.8 % |
| SpiderMonkey (Firefox) | 99.6 % |
| JavaScriptCore (Safari) | 99.4 % |
| QuickJS | ~97 % |
| Hermes (React Native) | ~95 % |
| **Phasis** | **100 %** |

Phasis sits ahead of every production JavaScript engine on raw conformance — including V8, SpiderMonkey, and JavaScriptCore. Those engines each carry a small residual gap (newly-landed proposals, rare edge cases under construction) that Phasis closes by virtue of being a single-purpose conformance-first implementation.

## Coverage by category

| Category | Pass | Fail | Skip |
|---|---:|---:|---:|
| `language/*` (syntax, semantics) | 23,384 | 0 | 0 |
| `built-ins/*` (standard library) | 22,729 | 0 | 0 |
| `intl402/*` (Intl spec) | 1,566 | 0 | 0 |
| `annexB/*` (legacy web features) | 1,079 | 0 | 0 |
| `staging/*` (incubating tests) | 1,632 | 0 | 0 |
| `harness/*` (harness self-tests) | 116 | 0 | 0 |

Every category, sub-category, and individual test in the upstream suite at the pinned revision passes.

## Notable edge cases

A few fixture areas require deliberate engine work to pass:

- **SpiderMonkey stress fixtures** — the `dst-offset-caching` shards, the Sputnik `decodeURI` / `decodeURIComponent` UTF-8 sweep, and the typed-array / nullish-coalescing assert loops run with a per-test budget bump via `test262_isolated_tests` in `config/support.php`. The bytecode VM handles the iteration counts each fixture exercises; the budget bump lets each one run to completion under PHP's tracing JIT.
- **Chinese-calendar uncommon-leap-month** — the `from-chinese-leap-month-uncommon.js` fixture asserts `Temporal.PlainMonthDay.from({calendar:"chinese", monthCode:"M09L"..."M12L", day:30})` succeeds; the resolved ISO reference years stretch back to -6482. Phasis's chinese / dangi calendar tables use a pure-PHP Reingold-Dershowitz extension (Calendrical Calculations §19; Meeus algorithms for solar / lunar longitude and new-moon estimation) covering extended-year -7500 through 3500 with no ICU dependency at codegen or runtime.

`HOST_GAP_BLOCKLIST` and `CROSS_REALM_BLOCKLIST` in `tests/Oracle/Test262Runner.php` are both empty.

## How we run it

```bash
./vendor/bin/test262 --jobs 4
```

Locally this takes ~20 min on a recent Mac. The CI matrix shards the suite across 73 parallel workers and finishes in ~7 min wall-clock.

The latest compatibility snapshot is committed to `COMPAT.md` on every successful CI run. See [the CI workflow](https://github.com/inline0/phasis/blob/main/.github/workflows/compat-matrix.yml) for the full matrix definition.

## Regressions are blocked at merge

`./bin/verify-all` runs the full quality gate locally:

- PHPStan level 6 (zero errors)
- PHP_CodeSniffer (zero warnings)
- PHPUnit unit suite (642 tests)
- Oracle scenarios (top-level categories in `scenarios/`)

Plus the test262 matrix on every PR. A change that drops the pass count is rejected automatically — and at 100 % the only way it can move is down, so the bar is unambiguous.
