#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "support-report is deprecated; generating compat.json + COMPAT.md instead." >&2
"$SCRIPT_DIR/compat-report" "$@"
